class Node:
    def __init__(self, val):
        self.value = val
        self.next = None

    def SetNext(self, node):
        self.next = node

    def printAllSequence(self):
        while(next != None):
            print self.value
            next = self.next
